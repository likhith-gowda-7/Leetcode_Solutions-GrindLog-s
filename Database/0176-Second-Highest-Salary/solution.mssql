--Write your MySQL query statement below
with details as(
    select salary, dense_rank() over(order by salary desc) as salary_rank
    from Employee
)
select max(salary) as SecondHighestSalary
from details where salary_rank=2;